# 实验记录

本文件记录模型、特征和评估流程的可复现实验变化。测试集只用于最终评估，不用于选择特征或报警阈值。

## 2026-09-23：瞬态特征结果后的评估改进

### 已知结果

- 基线测试集 AP：5/10/30 分钟分别为 `0.059/0.088/0.201`。
- 加入 `overthreshold_count` 后，30 分钟测试 AP 为 `0.405`，相对基线提升约 `102%`。
- 全部瞬态特征的30分钟 AP 为 `0.359`；STA-LTA 为 `0.221`；峰值/峰均比和短窗能量 q99 分别为 `0.187/0.186`。
- 原阈值 `0.5` 下测试事件检出为 `0/3`。这说明排序能力可能改善，但概率没有达到当前报警阈值，必须检查校准和阈值选择。
- Sundial 的连续特征预测误差比持值基线低约 `17%～22%`。该结果证明平均特征走势预测更好，不单独等价于岩爆事件检出能力。

### 本次代码修改

1. `rockburst/model.py`
   - 增加每个时窗的最大概率、正样本最大概率和负样本最大概率。
   - 增加验证/测试阈值扫描，记录事件检出率、报警段数、误报段数、误报率和报警时间占比。
   - 阈值扫描只提供诊断，不会用测试集自动调参。
2. `rockburst/workflow.py`
   - 瞬态消融结果按验证集30分钟 AP 自动给出 `recommended_variant`。
   - 在 `summary.json` 中明确记录选择规则，测试集只用于最终参考。
3. `README.md`
   - 补充 AP、阈值0.5下0/3检出的解释，以及验证集选型和测试集隔离要求。
4. `tests/test_pipeline.py`
   - 增加阈值扫描和事件级报警诊断的测试。

### 下一轮实验计划

1. 使用 `baseline + overthreshold_count` 作为首选候选，并保留 baseline、STA-LTA 和 all-transient 作为验证集对照。
2. 仅在验证集选择报警阈值；建议先查看 `metrics.json` 的 `threshold_sweep`，再固定阈值进行一次测试集评估。
3. 报告事件级首次报警时间、提前量、误报段数和检出率，不只报告样本级 AP。
4. 保持按记录文件夹分组的训练/验证/测试划分；测试事件数量增加前，所有结果都标记为探索性结果。
5. 另做“去掉 Sundial 预测特征”的对照，区分 Sundial 的平均趋势预测贡献和瞬态特征贡献。

## 2026-09-23：事件均衡训练与模型说明

### 问题判断

当前所有方案的正负概率集中在约 `0.32～0.40`，说明单纯降低报警阈值会造成长时间报警，升高阈值又会漏掉事件。这个问题不能用简单阈值解决，优先检查训练样本权重和正负样本的相关性。

### 代码修改

- `rockburst/model.py` 增加 `event_balanced_weights`：让训练集每个岩爆事件的正样本总权重相近，同时保留所有原始样本。
- `fit_hazard` 支持样本权重；原有训练方式保持兼容。
- `run` 增加 `--event-balanced`，用于与原模型做受控对照。
- 模型和报告保存 `event_balanced`、`balanced_training_events` 元数据。
- README 解释当前模型是三时窗离散时间风险模型，报警阈值只负责把概率转换成报警状态，并非特征硬阈值。

### 下一次实验命令

```bash
python -m rockburst run \
  --continuous-dir data/continuous \
  --microseismic-dir data/microseismic \
  --labels data/rockbursts.txt \
  --model-path models/sundial-base-128m \
  --device cuda \
  --output artifacts/event_balanced \
  --transient-ablation \
  --event-balanced
```

比较重点是原方案与事件均衡方案在验证集上的概率分离、阈值扫描和事件级提前量；不能只根据测试集 AP 选择方案。

## 2026-09-23：验证集自动阈值与连续报警

- `run` 新增 `--auto-threshold`：只用验证集30分钟阈值扫描选择工作点。
- 默认约束验证集孤立误报不超过2段，再最大化事件检出率；选择结果写入 `model.json`。
- 新增 `--min-active-bins {1,2,3}`，要求连续多个bin超过阈值才形成报警段，用于抑制孤立尖峰。
- 测试集只使用保存的阈值和连续报警规则，不参与调参。

## 2026-09-25：无可用工作点时不再选择全静默阈值

- 自动阈值选择增加 `--min-validation-recall`，默认要求验证集30分钟事件召回率至少为0.4。
- 如果验证集不存在同时满足召回和孤立误报约束的阈值，记录 `status=no_operating_point`，不再错误选择0.90等全静默阈值。
- `min_active_bins` 默认保持1，以保留可能低于60秒的短促岩爆信号。

## 2026-09-23：事件边界告警单独统计

测试集中有些告警只与真实事件相差一个30秒 bin。原实现把它们全部计入误报，导致“误报17.1/24h”容易被误解为17次实际误报。

本次评估调整：

- 保留严格的 `false_alarm_episodes`，保证历史指标可比较。
- 数据集保存每个样本对应事件的真实 `onset_time`，按预测窗口结束时间与 onset 的实际距离判断边界告警，不再依赖相邻样本作为唯一依据。
- 新增 `boundary_near_event_episodes`：事件 onset 刚好落在预测窗口边界容差内的告警。
- 新增 `post_event_carryover_episodes`：事件发生后一个刷新间隔内仍持续的告警。
- 新增 `isolated_false_alarm_episodes` 和对应的24小时换算速率，表示真正与事件无关的孤立误报。
- 阈值扫描表同步输出上述拆分结果。

这只改变误报的解释和报告方式，不修改训练标签、不把边界告警伪装成独立事件，也不改变模型概率。
