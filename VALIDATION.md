# 基础版验证记录

本次验证完成于 2026-09-20。

- 上游 GitHub 仓库已克隆到 `vendor/Sundial/`，提交为 `3ef03b8c3804f64a506e57101a173470040aaece`。
- 本机使用 Python 3 的现有 NumPy 2.5.2、SciPy 1.18.0。
- `python3 -m unittest discover -s tests -v`：24 项测试通过。
- 测试覆盖：小端 int32 读取、跨文件10秒窗口、截断文件拒绝、时区要求、未来波形/事件排除、延迟到达、缺失覆盖、事件去重检查、条件区间标签、进行中岩爆排除、概率单调性、模型序列化、推理缓存、时间边界30分钟标签剔除、事件群隔离，以及修改测试特征不影响训练参数。
- `python3 -m rockburst demo --output artifacts/demo`：合成特征+持值基线完成建样本、拟合、时间划分和报告，测试段1402个有效分钟。
- 单次预测命令完成，结果在 `artifacts/demo/prediction.json`。该文件是合成数据上的持值基线结果，不是 Sundial 或真实岩爆预测。
- 缺失输入预测命令返回 `status=insufficient_data` 和三个 `null` 概率，退出码2。
- 真实文件 `0008655-fs-eDAS-5000Hz-0041pt-20260621T151630.846.bin`，按5000Hz、41通道、监测15～34通道读取，成功提取2个完整UTC网格10秒块；首尾不足完整网格部分丢弃。输出在 `artifacts/real_bin_smoke/`。该读取验证的时区和可用时间是测试假设，不是训练标注。

尚未验证的内容：

- 未安装本项目所需的 PyTorch/Transformers 环境，未下载或运行 Sundial 模型权重；适配器依据官方接口编写，真实模型推理需在权重准备后验证。
- 未用25次真实岩爆训练、校准或测试；没有现场预测效果或硬件吞吐结论。
- 当前CLI是离线/单次预测基础版，不含在线文件监听和原有GUI集成。

重现命令与真实数据模板见 `README.md`。
