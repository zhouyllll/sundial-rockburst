# 基础版验证记录

## 2026-09-22：预测时刻可用性与岩爆标签配对

- 修正连续输入历史的可用时间检查：每个被使用的10秒特征块都必须在预测时刻已经可用，不能容许最多60秒的未来可用记录混入历史窗口。
- 新增回归测试覆盖“历史块在预测时刻后才写完”；本轮再加入来源文件夹正负样本同组、重叠输入窗口合并、整组时间切分测试，完整测试共40项通过。
- Windows守护脚本改为直接读取原始`label.txt`时刻范围，由主流程依据事件子文件夹bin的日期和时间唯一匹配；绕过旧`_make_labels.py`只按钟点最近邻配对的CSV。
- `run`现在按bin来源子文件夹为全部样本分组，并将时间重叠或30分钟输入窗口可能相交的文件夹合并；按组时间顺序切分约70/15/15。文件夹结构不足3个独立组时停止，避免偷偷退回泄漏的逐样本切分。平铺目录保留旧时间划分用于演示。
- 合成原始bin端到端流程通过，40项测试通过。该修正需要在包含真实G盘数据和Sundial权重的机器上重新运行完整训练；本机没有该数据，不能据此重算或宣称指标改善。


## 2026-09-22：微震分支可选

- `run`与`stream`的`--microseismic-dir`改为可选。省略时写出空事件特征、关闭微震分支，并将模式保存在数据集及模型元数据中；流式预测会要求与训练配置一致。
- 支持连续bin总目录下按事件分子文件夹存放，并从bin文件名日期补全时间范围标签；支持Windows盘符路径在WSL/Linux转换为`/mnt/<drive>/...`。
- `python3 -m unittest discover -s tests -v`：36项测试通过。
- 未下载或运行真实Sundial权重；本次验证是接口和合成测试。

## 2026-09-21：30分钟滑窗、一个bin一个步长

- `run`默认连续历史改成30分钟，训练样本时刻取每个bin的完成时间，保留毫秒，不再固定每分钟采样。
- 新增`stream`，对已有bin按时间流式回放；每读完一个bin更新窗口并立即写入JSONL、CSV和最新预测。不读取未来文件作为输入，不需要岩爆标签。
- 单小时合成bin测试验证第60个30秒文件产生首次预测，之后每个文件一次，共61次输出；连续特征窗口最多180个10秒块。
- 已确认岩爆恰在录像/采集末尾时，保留此前能够确定的正样本标签。
- 31项测试通过。
- 6小时合成bin按30秒步长完成训练，生成596个有效样本；相同数据流式回放输出661条有效预测。
- 90个测试时刻的流式结果与批量测试结果在三个时窗上均一致（绝对/相对容差1e-12）。
- 以上使用明确标记的持值基线，未执行真实Sundial权重；`stream`当前是文件时间回放，不是在线目录监听服务。

## 2026-09-21：目录到训练的主流程

- 新增 `run`：只输入连续bin目录、微震bin目录、岩爆时间txt，自动生成内部CSV并完成特征提取、预测特征、标签、时间划分、训练与测试。
- 新增 `demo-raw` 并实际执行：720个连续合成bin（6小时）+30个合成微震片段+5次合成岩爆，产出266个有效样本，完成模型训练、测试和最新一次预测。
- 本次整链路验证使用明确标记的 `persistence` 基线，未运行真实Sundial权重。
- 自动扫描直接支持现有 `5000Hz-0041pt-20260621T151630.846.bin` 命名。简化入口只需标签时间，不用手填四张CSV。
- `python3 -m unittest discover -s tests -v`：28项测试通过，新增目录扫描、文件名元数据、微震片段索引和纯文本岩爆标签测试。
- 简化入口按文件结束时刻可用、输入归档/标签完整处理；默认假设随结果保存在 `input_summary.json`。

## 初版记录

本次验证完成于 2026-09-20。

- 上游 GitHub 仓库已克隆到 `vendor/Sundial/`，提交为 `3ef03b8c3804f64a506e57101a173470040aaece`。
- 本机使用 Python 3 的现有 NumPy 2.5.2、SciPy 1.18.0。
- `python3 -m unittest discover -s tests -v`：24 项测试通过。
- 测试覆盖：小端 int32 读取、跨文件10秒窗口、截断文件拒绝、时区要求、未来波形/事件排除、延迟到达、缺失覆盖、事件去重检查、条件区间标签、进行中岩爆排除、概率单调性、模型序列化、推理缓存、时间边界30分钟标签剔除、事件群隔离，以及修改测试特征不影响训练参数。
- `python3 -m rockburst demo --output artifacts/demo`：合成特征+持值基线完成建样本、拟合、时间划分和报告，测试段1402个有效分钟。
- 单次预测命令完成，结果在 `artifacts/demo/prediction.json`。该文件是合成数据上的持值基线结果，不是 Sundial 或真实岩爆预测。
- 缺失输入预测命令返回 `status=insufficient_data` 和三个 `null` 概率，退出码2。
- 真实文件 `0008655-fs-eDAS-5000Hz-0041pt-20260621T151630.846.bin`，按5000Hz、41通道、监测15～34通道读取，成功提取2个完整UTC网格10秒块；首尾不足完整网格部分丢弃。输出在 `artifacts/real_bin_smoke/`。该读取验证的时区和可用时间是测试假设，不是训练标注。

尚未验证的内容：

- 未安装本项目所需的 PyTorch/Transformers 环境，未下载或运行 Sundial 模型权重；适配器依据官方接口编写，真实模型推理需在权重准备后验证。
- 未用25次真实岩爆训练、校准或测试；没有现场预测效果或硬件吞吐结论。
- 当前CLI是离线/单次预测基础版，不含在线文件监听和原有GUI集成。

重现命令与真实数据模板见 `README.md`。
